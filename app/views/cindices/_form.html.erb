<%= form_for(@cindex) do |f| %>
  <% if @cindex.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cindex.errors.count, "error") %> prohibited this cindex from being saved:</h2>

      <ul>
      <% @cindex.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
